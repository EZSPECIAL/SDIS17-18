LAB01 (Unicast)

datagram socket
inet address
	inet4
	inet6

InetAddress getByName(String hostname)

DatagramSocket()
DatagramSocket(int port)
DatagramSocket(int port, InetAddress addr)

send(DatagramPacket)
receive(DatagramPacket)

setSTimeout - socket timeout

DatagramPacket(bytes[] buf, int length)
DatagramPacket(bytes[] buf, int length, InetAddress addr, int port)

bytes[] getData()
InetAddress getAddress()
int getPort()

setData(bytes[])

String.getBytes()
String(bytes[])
trim()
split()
compareTo()

state machine
- send / wait / resend N times on timeout

LAB02 (Multicast)

MulticastSocket
joinGroup(InetAddress) -> notify that client wants to receive multicast messages
leaveGroup(InetAddress) -> leave multicast group
setTimeToLive(int ttl)

224.0.0.0
239.255.255.255

Server has to periodically send multicast message and then receive data, use Timer thread for multicast and main thread locks on receive()